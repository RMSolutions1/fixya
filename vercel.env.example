# Vercel — FixYa full stack (web + API NestJS + Supabase)
# Configurar en: Project → Settings → Environment Variables → Production

# ─── Públicas (frontend) ─────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://fixya.emprenor.com
# Vacío = misma URL /api/v1 (recomendado en Vercel)
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_ENABLE_SANDBOX_PAYMENTS=false
NEXT_PUBLIC_SUPABASE_URL=https://qsowfnlmpwjfrqjemsnt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ─── API NestJS (serverless) ─────────────────────────────────────
NODE_ENV=production
JWT_SECRET=                          # ≥32 caracteres, único
DATABASE_URL=                        # Pooler Supabase :6543
DIRECT_URL=                          # Session pooler :5432
APP_PUBLIC_URL=https://fixya.emprenor.com
API_PUBLIC_URL=https://fixya.emprenor.com
CORS_ORIGINS=https://fixya.emprenor.com
ENABLE_SWAGGER=false
ENABLE_SANDBOX_PAYMENTS=false
MP_ACCESS_TOKEN=                     # APP_USR-... (activar con: npm run mp:enable)
MP_WEBHOOK_SECRET=                   # Secret del webhook MP (mismo comando)
MP_SANDBOX=false
RESEND_API_KEY=                      # re_... (emails transaccionales)
API_PREFIX=v1
