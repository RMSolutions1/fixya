import { ValueProps } from '@/components/marketing/value-props';

/** @deprecated Preferir `<ValueProps variant="strip" />` directamente */
export function TrustBadges() {
  return <ValueProps variant="strip" />;
}
